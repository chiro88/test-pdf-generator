"""picker_cmc no-truth detector baseline (D11).

The detector reads ONLY the PDF (+ optional config). It never reads RTM
truth.json and never copies truth bboxes. Output conforms to detector-output-v0.
"""
