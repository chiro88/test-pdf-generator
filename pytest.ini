[pytest]
# Registered so `-m "not slow"` / `-m rtm_integration` work without warnings.
markers =
    slow: slower end-to-end tests (full gallery build, CLI subprocess)
    rtm_integration: RTM factory end-to-end integration gates (D6)
