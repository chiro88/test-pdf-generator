# Tests

These tests are smoke/contract tests for the v0 RTM factory. The cc completion task must add detector comparison tests and frozen-fixture tests.

Run from the bundle root:

```bash
python -m pytest tests
```
